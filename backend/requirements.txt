# ===== kefuxitong 后端依赖（精确版本锁定，保障新电脑可复现）=====
# 完整冻结清单（含所有传递依赖）见 requirements.lock.txt
# 生成方式：pip install -r requirements.txt && pip freeze > requirements.lock.txt
fastapi==0.141.1
uvicorn[standard]==0.52.1
sqlalchemy==2.0.52
pydantic==2.13.4
pydantic-settings==2.15.0
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-multipart==0.0.32
openai==3.0.0  # OpenAI 兼容 LLM 客户端（DeepSeek/通义/OpenAI 通用）
python-pptx==1.0.2  # PPT 解析
python-docx==1.2.0  # Word 解析
pdfplumber==0.11.10  # PDF 解析
openpyxl==3.1.5  # Excel 解析
